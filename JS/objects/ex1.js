// Create an object student with properties: name, age, and isEnrolled.

const student = {
    name: "Anas",
    age: 18,
    isEnrolled: true,
};

student.grade = "A";
student.age = 20;
delete student.isEnrolled;

console.log(student);
